name: Build Android APK

# Builds a release APK in the cloud so it can be downloaded and installed
# without needing a PC. Runs on every push to main, and can also be
# triggered manually from the Actions tab (useful from a phone/tablet
# browser: Actions -> Build Android APK -> Run workflow).
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install JS dependencies
        run: npm ci

      - name: Ensure gradlew is executable
        run: chmod +x android/gradlew

      - name: Build release APK
        working-directory: android
        run: ./gradlew assembleRelease --no-daemon

      - name: Upload Viosk APK
        uses: actions/upload-artifact@v4
        with:
          name: viosk-release-apk
          path: android/app/build/outputs/apk/viosk/release/app-viosk-release.apk
          retention-days: 30

      - name: Upload Akanan APK
        uses: actions/upload-artifact@v4
        with:
          name: akanan-release-apk
          path: android/app/build/outputs/apk/akanan/release/app-akanan-release.apk
          retention-days: 30
